# 后端 + GUI 依赖
#
# 注意：**必须 Python 3.13**。
# 原项目 main.py 里有 `from queue import ShutDown`，那是 Python 3.13 才加进来的
# （3.12 及以下会直接 ImportError: cannot import name 'ShutDown'）。
#
# 装法（在项目根）：
#     py -3.13 -m venv .venv-gui
#     .venv-gui\Scripts\python.exe -m pip install -r bridge\requirements.txt

# ---- GUI（PySide6 + Fluent Widgets）----
PySide6>=6.7
PySide6-Fluent-Widgets>=1.11

# ---- 原项目运行依赖（后端会 import 它）----
loguru>=0.7.3
tqdm>=4.67.1
requests>=2.32.5
pyaes>=1.6.1
beautifulsoup4>=4.14.2
lxml
fonttools>=4.60.1
openai>=1.109.1
httpx>=0.28.1
urllib3>=2.5.0
